{
  "name": "jug-nx",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "bundle": "nx run-many --target=bundle",
    "dev": "nx run-many --target=dev --verbose --output-style=stream",
    "serve": "pnpm bundle && java -jar ./bundle/dist/jug-nx.jar --spring.datasource.url=jdbc:h2:file:./data/demo",
    "test": "nx run-many --target=test"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@7.13.4",
  "devDependencies": {
    "nx": "^15.5.3"
  }
}
